// los objetos son la pieza principal de objetos en javascript

const nombre="monitor 20 pulgadas";
const precio=13123;
// podemos colocar todo eso en una variable objeto

// a esto se le conoce como object literal
const producto = {
    // las variables dentro de objetos son llamados propiedades
    nombre2:"jose",
    // no es ; es una ,(coma)
    apellido:"diaz",
}

console.log(producto);