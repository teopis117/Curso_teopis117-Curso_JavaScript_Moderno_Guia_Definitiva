const card=document.querySelectorAll('.card');
console.log(card);

// nos retornara un nodelist que es una lista de todos los elementos que encuentre

// si un elemento no existe nos retornan un nodelist vacio