// el operador if es una de las bases para poder conocer estructuras a lo largo de cualquier lenguaje de programaciono en su mayoria

// Operador if
const puntaje = '1000';
// if(condicion)
// es igual
if(puntaje == 1000) {
    console.log("Si es igual!");
} 
//  else se llevara acabo cuando la condicion del if no sea valida y ejecutara el codigo dentro de sus llaves
else {
    console.log("No no es igual");
}
