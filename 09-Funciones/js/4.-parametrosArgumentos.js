// a esta funcion le pasamos 2 parametros
function sumar(a,b)
{
    console.log(a+b);
}

// estos son los argumentos de una function en general seran los valores reales con los cuales funcionara el programa
sumar(2,3);
                // nombre y apellidos son nuestros paramentros
function saludar(nombre,apellido)
{
    console.log(`hola ${nombre} ${apellido}`);
}

// estos son los argumentos de la funcion
saludar('jose','diaz');
// saludar(jose,diaz); si mandamos algo como esto esta por que esperara una variable cuando debemos de enviar un valor

